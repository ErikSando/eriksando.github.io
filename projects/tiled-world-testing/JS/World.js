const World = {
    blocks: [],
    blockPositions: [],

    load(data) {

    },

    Draw(ctx) {

    }
}

function Raycast(start, direction, maxDistance, info = new RaycastInfo()) {
    let ray = new Ray(start, direction);

    let intersections = [];
    
    
}